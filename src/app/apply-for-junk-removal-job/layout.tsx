import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Apply for Junk Removal Contractor Position — 1099, Truck or Trailer Required",
  description: "Apply to run jobs as a 1099 junk removal contractor. $100/hr per man + 50% of items we resell. Pick your own schedule. Must have at least a trailer.",
  alternates: { canonical: "/apply-for-junk-removal-job" },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
